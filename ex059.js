/* Faça um programa que leia 5 números e informe o maior número */

let maiorNum = (num1, num2, num3, num4, num5) => {
    return Math.max(num1, num2, num3, num4, num5)
}
console.log(maiorNum(200, 30, 3, 90, 2));